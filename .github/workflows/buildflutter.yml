name: Build Flutter APK

on:
  workflow_dispatch:
    inputs:
      zip_url:
        description: "URL download ZIP project Flutter"
        required: true
        type: string
      tag:
        description: "Tag build (unik per user/job)"
        required: true
        type: string
      build_type:
        description: "debug atau release"
        required: true
        type: string
        default: "release"

env:
  TAG: ${{ inputs.tag }}
  BUILD_TYPE: ${{ inputs.build_type }}
  ZIP_URL: ${{ inputs.zip_url }}

jobs:
  build:
    name: Flutter Build (${{ inputs.build_type }})
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          cache: true

      - name: Create workdir
        run: |
          mkdir -p "$GITHUB_WORKSPACE/project" "$GITHUB_WORKSPACE/out" "$GITHUB_WORKSPACE/logs"
          echo "TAG=${{ inputs.tag }}" > "$GITHUB_WORKSPACE/logs/build_meta.txt"
          echo "BUILD_TYPE=${{ inputs.build_type }}" >> "$GITHUB_WORKSPACE/logs/build_meta.txt"
          echo "PLATFORM=flutter" >> "$GITHUB_WORKSPACE/logs/build_meta.txt"
          echo "STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_WORKSPACE/logs/build_meta.txt"

      - name: Download project ZIP
        id: download
        run: |
          set -e
          LOG="$GITHUB_WORKSPACE/logs/build_error.log"
          {
            echo "========== DOWNLOAD ZIP =========="
            echo "URL: ${{ inputs.zip_url }}"
            echo "TAG: ${{ inputs.tag }}"
            echo "TIME: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo ""
          } | tee -a "$LOG"

          curl -L --fail --retry 3 --retry-delay 2 \
            -o "$GITHUB_WORKSPACE/project.zip" \
            "${{ inputs.zip_url }}" 2>>"$LOG" || {
            echo "ERROR: Gagal download ZIP dari URL yang diberikan." | tee -a "$LOG"
            exit 1
          }

          FILE_SIZE=$(stat -c%s "$GITHUB_WORKSPACE/project.zip" 2>/dev/null || echo 0)
          echo "Downloaded size: ${FILE_SIZE} bytes" | tee -a "$LOG"

          if [ "$FILE_SIZE" -lt 100 ]; then
            echo "ERROR: File ZIP kosong atau terlalu kecil." | tee -a "$LOG"
            exit 1
          fi

      - name: Extract & locate Flutter project
        id: extract
        run: |
          set -e
          LOG="$GITHUB_WORKSPACE/logs/build_error.log"
          {
            echo ""
            echo "========== EXTRACT ZIP =========="
          } | tee -a "$LOG"

          unzip -q -o "$GITHUB_WORKSPACE/project.zip" -d "$GITHUB_WORKSPACE/project" 2>>"$LOG" || {
            echo "ERROR: Gagal extract ZIP. Pastikan file valid (.zip)." | tee -a "$LOG"
            exit 1
          }

          # Cari pubspec.yaml (bisa nested 1-2 level)
          PUBSPEC=$(find "$GITHUB_WORKSPACE/project" -maxdepth 4 -type f -name "pubspec.yaml" | head -n 1)

          if [ -z "$PUBSPEC" ]; then
            echo "ERROR: pubspec.yaml tidak ditemukan di dalam ZIP." | tee -a "$LOG"
            echo "Struktur folder setelah extract:" | tee -a "$LOG"
            find "$GITHUB_WORKSPACE/project" -maxdepth 3 -type d 2>/dev/null | head -50 | tee -a "$LOG"
            exit 1
          fi

          PROJECT_DIR=$(dirname "$PUBSPEC")
          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"
          echo "Found pubspec.yaml at: $PUBSPEC" | tee -a "$LOG"
          echo "Project root: $PROJECT_DIR" | tee -a "$LOG"

          # Info ringkas
          if [ -f "$PROJECT_DIR/pubspec.yaml" ]; then
            echo "--- pubspec.yaml (head) ---" | tee -a "$LOG"
            head -30 "$PROJECT_DIR/pubspec.yaml" | tee -a "$LOG"
          fi

      - name: Flutter doctor
        run: |
          flutter --version
          flutter doctor -v || true

      - name: Flutter pub get
        id: pubget
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          set -e
          LOG="$GITHUB_WORKSPACE/logs/build_error.log"
          {
            echo ""
            echo "========== FLUTTER PUB GET =========="
          } | tee -a "$LOG"

          flutter pub get 2>&1 | tee -a "$LOG"
          echo "pub get OK" | tee -a "$LOG"

      - name: Build APK
        id: build
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          set -e
          LOG="$GITHUB_WORKSPACE/logs/build_error.log"
          TYPE="${{ inputs.build_type }}"
          {
            echo ""
            echo "========== FLUTTER BUILD ($TYPE) =========="
            echo "TIME: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          } | tee -a "$LOG"

          if [ "$TYPE" = "debug" ]; then
            flutter build apk --debug 2>&1 | tee -a "$LOG"
            APK_SRC=$(find build/app/outputs -type f -name "*.apk" 2>/dev/null | head -n 1)
          else
            flutter build apk --release 2>&1 | tee -a "$LOG"
            APK_SRC=$(find build/app/outputs -type f -name "*release*.apk" 2>/dev/null | head -n 1)
            if [ -z "$APK_SRC" ]; then
              APK_SRC=$(find build/app/outputs -type f -name "*.apk" 2>/dev/null | head -n 1)
            fi
          fi

          if [ -z "$APK_SRC" ] || [ ! -f "$APK_SRC" ]; then
            echo "ERROR: APK tidak ditemukan setelah build." | tee -a "$LOG"
            echo "Isi build/app/outputs (jika ada):" | tee -a "$LOG"
            find build -type f -name "*.apk" 2>/dev/null | tee -a "$LOG" || true
            find build/app/outputs -type f 2>/dev/null | head -40 | tee -a "$LOG" || true
            exit 1
          fi

          OUT_NAME="app-${TYPE}-${{ inputs.tag }}.apk"
          cp "$APK_SRC" "$GITHUB_WORKSPACE/out/$OUT_NAME"
          ls -lh "$GITHUB_WORKSPACE/out/$OUT_NAME" | tee -a "$LOG"
          echo "APK_PATH=$GITHUB_WORKSPACE/out/$OUT_NAME" >> "$GITHUB_ENV"
          echo "Build sukses: $OUT_NAME" | tee -a "$LOG"

      - name: Write success log
        if: success()
        run: |
          LOG="$GITHUB_WORKSPACE/logs/build_error.log"
          {
            echo ""
            echo "========== BUILD SUCCESS =========="
            echo "TAG: ${{ inputs.tag }}"
            echo "PLATFORM: flutter"
            echo "BUILD_TYPE: ${{ inputs.build_type }}"
            echo "FINISHED_AT: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          } | tee -a "$LOG"
          cp "$LOG" "$GITHUB_WORKSPACE/out/build_log_${{ inputs.tag }}.txt"

      - name: Capture failure log
        if: failure()
        run: |
          LOG="$GITHUB_WORKSPACE/logs/build_error.log"
          {
            echo ""
            echo "========== BUILD FAILED =========="
            echo "TAG: ${{ inputs.tag }}"
            echo "PLATFORM: flutter"
            echo "BUILD_TYPE: ${{ inputs.build_type }}"
            echo "FAILED_AT: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo ""
            echo "--- flutter doctor (debug) ---"
            flutter doctor -v || true
            echo ""
            echo "--- last 80 lines of log ---"
            tail -80 "$LOG" || true
          } | tee -a "$LOG"
          mkdir -p "$GITHUB_WORKSPACE/out"
          cp "$LOG" "$GITHUB_WORKSPACE/out/build_error_${{ inputs.tag }}.log"
          cp "$LOG" "$GITHUB_WORKSPACE/out/build_log_${{ inputs.tag }}.txt"

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: apk-${{ inputs.tag }}
          path: ${{ github.workspace }}/out/*.apk
          if-no-files-found: error
          retention-days: 3

      - name: Upload build log artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: log-${{ inputs.tag }}
          path: |
            ${{ github.workspace }}/out/build_log_*.txt
            ${{ github.workspace }}/out/build_error_*.log
            ${{ github.workspace }}/logs/build_error.log
          if-no-files-found: ignore
          retention-days: 7
